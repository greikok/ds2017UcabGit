@model IEnumerable<DoctorWebASP.Models.Supervisor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Area.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Horarios)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Area.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Horarios)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SupervisorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SupervisorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SupervisorId })
        </td>
    </tr>
}

</table>
