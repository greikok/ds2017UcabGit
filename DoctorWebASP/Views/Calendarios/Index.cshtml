@model IEnumerable<DoctorWebASP.Models.Calendario>

@{
    ViewBag.Title = "Index";
}

<h2>Calendario</h2>

<p>
    Poner flechas aqui
</p>
<!--
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoraInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraFin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cancelada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Disponible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoraInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoraFin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cancelada)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Disponible)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CalendarioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CalendarioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CalendarioId })
        </td>
    </tr>
}

</table>
    -->
